#ifndef MENU_H
#define MENU_H

// Declaração das funções que serão expostas para o main.c
void DesenharMenuPrincipal(int opcaoFocada, int screenWidth, int screenHeight);
void DesenharMenuPausa(void);

#endif